write a code that can be compiled as linux and windows executables which should perform the following actions for acoustic Doppler velocimetry (ADV) measurements like they are obtained with a Nortek Vectrino or UBERTONE measurements:
1-read the input file `<input-data/NAME>` which contain raw measurement data from ADV probes (proms)
2-process these files to plot a time series of these data, notably U, V, and W velocities or U, V, W1 and W2 measurements (bsaed on user setting)
3-export graphs and stats.
Update the .gitignore file for the most suitable coding framework;
The executables should show a graphical user interface (gui), with one interactive flume surface (interactive frame) that shows a theoretical flume with corrdinate reference system origina place in the center-left of the flume; that is at the center inlet, x=0, y=0, and z=0 at the bottom pointing upward direction (orographic left bank=negative, right bank=positive) from the top with flow direction arrow indicated from left to right in the background. the flume interface window should be ratio 1:5, with light blue background color. users should be able to click into the interface to define one measurement point, first qualitatively through the click, second a wizard should open with x-y-z position fields (float values in meters, up to 4 digits precision) plus one input data file name pere xyz point --so there can be multiple point assigned to one top-view which should have as many round circle-markers as tehre are z-position and for each point on the xy-plane the should be able to enter a total water depth (in the point import wizard where setting the water depth at one xy-position imposes/overwrite the water depth for all other points with the exact same xy position) -- if water depth is defined the by-defualt-dark-gray circles should turn dark blue for that xyz position. for these time series analysis, add options to set a start and end time that a user can define for each xyz point. after pressing OK on the wizard button, in the bottom, a plot interface should fill showing all data series contained in the user data file, with color-blind friendly colormap options for that one measurement point to visualize all columns in that data file -- users should be able to select the data columns from a drop down menu and modify plots regarding line width, color, and style (closed, dotted, dashes, dot-dashes) -- same for plot markers (off, rectangular, circular, triangular, filled, line width, color);
next, users should be able to add more points in the flume frame and superposition them in the plot interface; so there should be a drop-down menu for choosing the measurement point under consideration left of the data series (column) in the x-y-z measurement point , in addition to a TKE-like fluctuation term them stems from the per-file per velocity component fluctuation, meaning something like 0.5*(STDEV(U)²+STDEV(V)²+STDEV(W)²); any point defined should be able to be modified at any time (regarding position and data inpur file). 
there should be despiking filters, notably the following options,
* linear interpolation to fill gaps
* correlation score threshold (average correlation=70 by default);
* Signal to Noise Ratio threshold (default filter value: clean everythin where SNR is < 20)
* velocity threshold: cleanup 3 where abs(U,V,W) > than <USER-VALUE> times STD(U,V,W)
* implement the spike filters and options that I integrated in my code in `/tke-calculator-main/rmspike.py`
* investigate more filters from https://github.com/farzadasgari/proadv
Also, there should be an additional, vertical plot frame that plots the U, V, and W components, so a plot where the user can switch on the vertical axis between z and h/z and activate U,V,W components and show in a top-right legend panel statistics, namely mean STDEV, skewness, kurtosis, and stresses, namely u'v', u'w', and v'w' as well as TKE and its dissipation (eps). these life profiles should be used to enable then heading (experimenter noise through wrong probe rotation around z-axis), roll (experimenter noise through wrong probe rotation around z-axis), and pitch (experimenter noise through wrong probe rotation around x-axis) corrections that the user should be able to apply for each xy-point, that is, correction to all point on that z-profile so that V and W means can be set to zero (the software should propose a value to get that target but there should also be a "set manually" option,) -- this is necesssary because the ADV probe might be have wrongly placed in the flume by the experimenter -- so check if there are even smarter options for this auto (angle/coordinate system correction) as for instance from $u' &= u\cos\theta + v\sin\theta; v' &= -u\sin\theta + v\cos\theta; \overline{w}=0 $.
the gui should have an option to add another plot frame below the default plot frame and an option to save one or the two plot frames as high-resolution png file (300 dpi), exactly as they are currently visualized; more over, provide an export csv option to export all currently shown (and/or angle-corrected data) data columns; and provide an export option for statistics per measurement x-y-z point over time and x-y vertical stats (i.e. mean STDEV, skewness, kurtosis) and stresses (namely u'v', u'w', and v'w') as well as TKE and its dissipation (eps) with absolut (z) and relative (z/h) depth columns -- the stats per vertical profile should also be exportable in a template excel file `/templates/ADV-profiles.xlsx` that also evaluates total velocity magnitude and direction (in angle-degrees relative to the bulk flow coordinate reference system defined in the flume above)
Any setup should be able to be saved as `.advProj` file that internally stores all point settings including data file contents, and plot settings so such one project file can work standalone and won't crash when opened on another computer with different relative data paths; so there should be something like a top menu bar with "File" that provides the load/save project options; one "Export" menu (Data > csv; Plots > png), one "Processing" menu to choose (and effectively set) how many CPUs are used for data processing though that should never me more than what is available on the computer, and one "About" item that shows the license, a link to the GitHub source repo and author (date) and software version info. there should also be an "Import" menu that directly opens a wizard to select multiple adv files at once, then, in an overview frame, define x, y, and z coordinates for each measurement file.
For the background image of the software use `/img/app-background.jpg` and for the logo/icon use `/img/logo.ico`; the entire interface should have a modern App look.
In this repo, complete the README with requirements, installation, and usage instructions
